import React from 'react';
import { StyledBars } from './Bars.styles';

const Bars = ({ show }) => {
  return (
    <StyledBars show={show}>
      <div />
      <div />
      <div />
    </StyledBars>
  );
};

export default Bars;
